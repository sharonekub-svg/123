# PitchVision — מסמך תכנון (DESIGN)

> מקבל וידאו של משחק כדורגל ומפיק אוטומטית סטטיסטיקה פר-שחקן + מפות חום + radar.
> "Veo, אבל עם ניתוח אירועים וסטטיסטיקה אישית."

---

## 1. מה זה

PitchVision מקבל וידאו של משחק כדורגל ומפיק אוטומטית:

- **אירועים פר-שחקן:** מסירות (+% הצלחה), מסירות מפתח, איבודים, חטיפות (חילוצים), גולים, אסיסטים.
- **מדדים פיזיים:** מרחק ריצה, מהירות, מפת חום, אחוז החזקת כדור.

`pitch` = מגרש כדורגל (לא "פיץ'" למשקיעים).

## 2. משתמש ובעיה

קבוצות חובבניות / אקדמיות / מאמנים / שחקנים. היום: או וידאו גולמי מהטלפון בלי ניתוח,
או חומרת Veo יקרה שנותנת בעיקר וידאו והיילייטס — לא סטטיסטיקה פר-שחקן ברמת אירועים.
תיוג ידני = שעות עבודה. אנחנו עושים את זה אוטומטית + אישור אנושי קל (HITL).

## 3. החלטות מוצר נעולות (לא לפתוח מחדש)

1. **מקור וידאו ב-MVP = העלאת קובץ (offline).** לא live, לא מצלמה ייעודית. הערך הוא ב-CV/סטטיסטיקה.
   אותו מנוע ירוץ בהמשך גם על live וגם על מצלמה — רק המקור מתחלף.
2. **פורמט = Web app.**
3. **ספורט = כדורגל**, עם ארכיטקטורה גנרית להרחבה.
4. **חצי-אוטומטי (HITL):** המערכת מנתחת לבד, המשתמש מאשר/מתקן זיהוי שחקנים ואירועים קריטיים.

## 4. פיצ'רים ל-MVP

1. העלאת וידאו משחק.
2. זיהוי אוטומטי של שחקנים / כדור / קבוצות.
3. סטטיסטיקה פר-שחקן.
4. נגן וידאו עם overlay + תצוגת radar (top-down) + timeline אירועים.
5. תיקון חצי-אוטומטי (HITL).

## 5. ארכיטקטורה

```
┌─────────────────┐     ┌──────────────────┐     ┌─────────────────────┐
│   Web (Vercel)  │     │  Object Storage  │     │   GPU Worker        │
│   Next.js + TS  │◄───►│   (R2 / S3)      │◄───►│   Python CV pipeline │
│ auth, ניהול,     │     │  וידאו + תוצרים   │     │ YOLO11→ByteTrack→   │
│ viewer, HITL     │     └──────────────────┘     │ teams→homography→   │
└────────┬────────┘                               │ events              │
         │                                        └──────────┬──────────┘
         ▼                                                   │
   ┌──────────────┐              ┌──────────┐               │
   │  Postgres    │◄─────────────┤  Queue   │◄──────────────┘
   │ metadata,    │              │  (jobs)  │
   │ events,stats │              └──────────┘
   └──────────────┘
```

**כלל מנחה:** Vercel = ניהול/תצוגה/CRUD בלבד. כל מה שכבד (וידאו + CV) רץ על **worker GPU נפרד**
(Modal / Replicate / RunPod) וזורם דרך object storage + queue.
ה-DB מחזיק metadata + אירועים + סטטיסטיקה מצרפית; **טראקים פר-פריים נשמרים כקבצי artifact** ב-storage.

### זרימת חיים של משחק

```
1. upload video        → R2, נוצר match (status: uploaded)
2. enqueue job         → worker מושך את הווידאו
3. pipeline רץ          → detection → tracking → teams → homography → events
4. כותב תוצרים          → tracks/radar/heatmaps → R2 ; events+stats → Postgres
5. status: needs_review → המשתמש ממפה שחקנים + מאשר אירועים קריטיים (HITL)
6. status: confirmed   → חישוב סטטיסטיקה סופי
```

## 6. ה-Pipeline הטכני (stack מוכח — SoccerNet 2023–2025, roboflow/sports)

| שלב | טכנולוגיה | הערות |
|-----|-----------|-------|
| זיהוי | **YOLO11** | שחקן/שוער/שופט/כדור. שחקנים 95%+. הכדור = הקשה: מודל ייעודי + SAHI slicing + אינטרפולציית מסלול |
| מעקב | **ByteTrack** (`supervision`) | ID יציב לכל שחקן |
| Re-ID | חצי-אוטומטי ב-MVP | הרכיב הכי קשה. המשתמש מצמיד פעם אחת לסגל |
| שיוך קבוצות | **SigLIP → UMAP → KMeans** | חלוקה ל-2 קבוצות לפי מראה. שוער בנפרד |
| מספר חולצה | OCR / VLM (**Qwen2-VL**) | מזין את ה-Re-ID |
| Homography | **YOLO11-pose** (~32 נק' מגרש) | מטריצת 3×3 → top-down + מרחק/מהירות. מצלמה סטטית רחבה = קל בהרבה |
| אירועים | מנוע **היוריסטי** על מסלולים | קרוב לכדור=החזקה; שינוי החזקה=מסירה/איבוד/חטיפה; כדור בשער=גול. בהמשך GCN |

## 7. רמות דיוק (כנה)

- **מצוין → טוב מאוד:** מיקום, מרחק, מהירות, מפות חום, מסירות, החזקה, גולים.
- **טוב, צריך כיול:** חטיפות, אסיסטים.
- **בינוני:** מסירות מפתח, Re-ID מושלם → **לכן HITL**.

## 8. מודל נתונים (JSON אחיד בין מנוע ל-Web)

קואורדינטות **תמיד במטרים על המגרש** (מנתק את הלוגיקה מזווית/רזולוציית המצלמה).

```jsonc
{
  "match":   { "id", "date", "home_team", "away_team", "fps", "duration_s", "pitch": {"length_m","width_m"} },
  "players": [ { "id", "team", "label", "color", "number" } ],
  "frames":  [ { "t", "ball": [x, y] | null, "positions": [ {"player", "x", "y"} ] } ],
  "events":  [ { "t", "type", "player", "target?", "outcome?" } ],
  "stats":   { "<player_id>": { "passes", "passes_completed", ... } }
}
```

המימוש הקנוני: `analyzer/model.py`. הסכמה הזו היא **מקור האמת** המשותף ל-Python ול-Web.

## 9. Roadmap

- **שלב 0 — שלד רץ (זה מה שאנחנו בונים עכשיו):** מבני נתונים + מנוע אירועים/סטטיסטיקה +
  **מצב סימולציה** (נתונים סינתטיים, end-to-end בלי GPU) + web viewer.
- **שלב 1:** CV אמיתי על קליפ קצר (YOLO11 + ByteTrack).
- **שלב 2:** קבוצות + homography + radar אמיתי.
- **שלב 3:** מסך HITL לאישור/תיקון.
- **שלב 4:** מספרי חולצה + Re-ID משופר.
- **שלב 5:** אירועים מתקדמים (GCN), מסירות מפתח, דוחות פר-שחקן.
- **בהמשך:** live, מצלמה ייעודית, קלט מ-Veo.

## 10. תשלומים

מנוי בהמשך (לפי קבוצה/חודש או לפי משחק). ב-MVP — שכבה חינמית.

## 11. מבנה ריפו

```
pitchvision/
├── DESIGN.md          # המסמך הזה
├── analyzer/          # ליבת Python: מודל + מנוע + סימולציה (רץ על worker GPU בהמשך)
│   ├── model.py       # מודל הנתונים הקנוני (JSON אחיד)
│   ├── engine.py      # מנוע אירועים + סטטיסטיקה היוריסטי
│   ├── simulate.py    # גנרטור נתונים סינתטיים (בלי GPU)
│   └── cli.py         # CLI: מייצר sample-match.json ל-web
└── web/               # Next.js + TS viewer (Vercel): radar + timeline + stats
```
